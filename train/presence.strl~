module presence :

inputoutput FREINER : integer; % input ?
input ACCELERER : integer;
inputoutput PRESENCE;
  
  loop

%    present PRESENCE   % inutile + erreur
%      then emit PRESENCE
%    end present;

    present FREINER 
     then  emit PRESENCE
    end present;

    present ACCELERER
     then emit PRESENCE
    end present;
    
  each tick
  
end module
