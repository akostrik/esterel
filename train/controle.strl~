module controle :

input ACCELERER : integer;
inputoutput FREINER : integer; 
input PRESENCE;
output VITESSE : integer;

  loop

%    present PRESENCE   % inutile + erreur
%      then emit PRESENCE
%    end present;

    present FREINER 
     then  emit PRESENCE
    end present;

    present ACCELERER
     then emit PRESENCE
    end present;
    
  each tick
  
end module
